Usage:	pushCode [-h] [-?] [--help]

The script automates work with Git and pushes files to GitHub.
If you want the script to work, toCode command should be done first.
You can change configuration files in .toCode.config file.
There is a symbolic link between .toCode.config and .pushCode.config.

Options:
 -h,-?,--help	display this help and exit

Exit status:
 0	if OK
 1	if argument is not recognised
